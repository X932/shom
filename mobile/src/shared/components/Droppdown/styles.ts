import { StyleSheet } from 'react-native';

export const dropdownStyles = StyleSheet.create({
  dropdownContainer: {
    width: '100%',
  },
  dropdown: {
    height: 50,
    borderColor: 'gray',
    borderWidth: 0.5,
    borderRadius: 8,
    paddingHorizontal: 8,
  },
  listContainer: {
    borderRadius: 8,
  },
  dropdownText: {
    fontSize: 16,
  },
  inputSearch: {
    height: 40,
    borderRadius: 8,
  },
});
